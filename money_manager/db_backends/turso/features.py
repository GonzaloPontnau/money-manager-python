from django.db.backends.sqlite3.features import DatabaseFeatures as SQLiteDatabaseFeatures

class DatabaseFeatures(SQLiteDatabaseFeatures):
    # Heredar todas las características de SQLite
    pass
