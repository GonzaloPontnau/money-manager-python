export default function HomePage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "Arial, sans-serif" }}>
      <h1>Money Manager Frontend Web</h1>
      <p>Base de migracion a frontend separado con Next.js + TypeScript.</p>
      <p>
        Configura <code>NEXT_PUBLIC_API_BASE_URL</code> para consumir la API v1 del backend.
      </p>
    </main>
  );
}
